// Stub for @icr/polyseg-wasm — the WASM segmentation module is loaded
// dynamically at runtime only when needed. This stub satisfies any
// static import without bundling the heavy WASM binary at build time.
export default {};
